/* Injected as a classic <script src> by the shell. No fetch, no module. */
Shell.registerEntry({
  id: 'f2-lens',
  index: 'F2',
  title: 'Heavy block',
  section: 'lenses',
  status: 'canonical',
  lane: 'fragment',
  style: 'spike',
  tags: ['fragment', 'iframe'],
  frame: { designWidth: 1100, aspect: '1100/900', previewHeight: 900 },
  thumb: { crop: [0.34, 40] },
  text: '<p>A standalone lens document mounted by <code>src=</code>. It owns its own faces via <code>content/_styles/spike.css</code>.</p>'
});
