/* atom stub — ck-e2. Declared in encyclopedia/manifest.js entries[] inline; the
   swatch renderer lives in encyclopedia/swatches.js keyed by atom id. This file
   exists so verifyManifests' folder-presence check passes. */
