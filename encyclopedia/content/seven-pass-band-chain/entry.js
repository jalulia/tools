/* seven-pass-band-chain — ck-e1 stub. ck-e2 folds in engine files, params and a swatch page.
   Declared in encyclopedia/manifest.js entries[] inline; this file exists so
   verifyManifests' folder-presence check passes and so the ck-e2 fold has a
   place to land. Registration happens from the manifest, not from here. */
