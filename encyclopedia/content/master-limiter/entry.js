/* master-limiter — ck-e5 audio atom stub. Declared inline in encyclopedia/manifest.js;
   this file exists so verifyManifests' folder-presence check passes and so
   ck-e5's swatch is drawn from swatches.js. Registration happens from the
   manifest, not from here. */
