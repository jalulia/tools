/* moire-aliasing — Gardener image-processing stub. Declared inline in
   encyclopedia/manifest.js entries[]; this file exists so verifyManifests'
   folder-presence check passes. The technique overprints two identical line
   gratings turned by a small angle and lets them beat — one periodic field
   sampling another, the moiré made from its cause, not painted on. Atom-free;
   the dot-screen-20 / bayer8 relation is prose (no `couples` schema key).
   Registration happens from the manifest, not from here.
   Specimen: claude/proposals/moire-aliasing.html */
